# Frontend for API interaction

Simple React frontend for interacting with the API.

## Prerequisites

- Node.js 18+ and npm

## Setup

```powershell
npm install
```

## Running
```
npm run dev
```

### Backend Connection locally

The frontend expects the backend to run on http://localhost:8000. Make sure CORS is enabled in the backend for http://localhost:3000.

